<div class="container">
  <% @action = 'update' %>
  <%= render "form" %>
  <% if admin? %>
    <%= link_to 'Delete', product_path(@product), method: 'delete', class: 'btn btn-danger' %>
  <% end %>
</div>
